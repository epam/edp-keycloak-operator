apiVersion: kuttl.dev/v1beta1
kind: TestStep
commands:
  - command: helm install --set image.repository=keycloak-image --set image.tag=latest --replace --wait keycloak-operator-e2e ../../../deploy-templates
    namespaced: true
